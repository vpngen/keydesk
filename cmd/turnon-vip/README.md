# Protocol0 turn ON

## Usage

`/opt/vgkeydesk/turnon-vip [options] [on|off]`

* `-id` - (for test only) brigade id (base32 format)
* `-d` - (for test only) directory with brigade files, default is `/home/<BrigadeID>`